<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% articles.each do |article| %>
      <tr>
        <td><%= link_to user_path(article.user) do %>
          <%= attachment_image_tag(article.user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
          <% end %>
        </td>
        <td><%= link_to article.title, article_path(article), class: "article_#{article.id}" %></td>
        <td><%= article.body %></td>
        <%# if article.favorited_by?(current_user) %>
          <!--<td>-->
          <%#= link_to article_favorites_path(article), method: :delete, remote: true do %>
            <!--♥<%#= article.favorites.count %>-->
          <%# end %>
          <!--</td>-->
          <%# else %>
          <!--<td>-->
          <%#= link_to article_favorites_path(article), method: :post, remote: true do %>
            <!--♡<%#= article.favorites.count %>-->
          <%# end %>
          <!--</td>-->
          <%# end %>
        <td id="favorites_buttons_<%= article.id %>">
        <%= render partial: 'favorites/favorite', locals: { article: article} %>
        </t>
        <td>コメント数：<%= @article.user_comments.count %></td>
      </tr><%#= link_to " コメント数：#{article.user_comments.count}",article_path(article.id) %>
    <% end %>
  </tbody>
</table>